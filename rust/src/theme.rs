//! Colour themes (Phase C).
//!
//! Ports `themes.py` — and, unlike the Python version where the module was
//! never wired in, actually applies the palette to `render.rs` and the ratatui
//! widget styles so theme switching works.
